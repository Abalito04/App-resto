[phases.setup]
nixPkgs = ["python312", "gcc", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --access-logfile - --access-logformat '%({X-Forwarded-For}i)s \"%m %U\" %s %b %(D)sus'"
